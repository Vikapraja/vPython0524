#string formatting(f-string)
    #It is a way for using variable & expression in a string
    #we using fallowing 2 things:
        #f as prifix
        #{variable} or {expression}

a=int(input('enter a='))
b=int(input('enter b='))
mul=a*b
print(f"multiplication of {a} & {b}={a*b}")
